/**
 * Root page that loads the map UI.
 * In the future, other pages can be added under /app for additional routes.
 */
export default function HomePage() {
    return null; // layout.jsx already renders everything
  }
  